import ReferralSystem from '../components/ReferralSystem';

const ReferralPage = () => {
  return (
    <main className="pt-10">
      <ReferralSystem />
    </main>
  );
};

export default ReferralPage;


